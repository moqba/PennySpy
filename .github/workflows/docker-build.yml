name: Docker build

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  docker-build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up uv
        uses: astral-sh/setup-uv@v5

      - name: Update uv lockfile
        run: uv lock

      - name: Build Docker image
        run: docker build -t pennyspy:pr-check .
